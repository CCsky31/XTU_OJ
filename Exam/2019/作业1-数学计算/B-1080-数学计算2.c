#pragma warning(disable:4996)； //关闭scanf不可用的提示
//以下为代码正文

#include <stdio.h>
#include<math.h>
int main() {
    float a = sqrt(pow(3.2,2) + pow(4.7,2));
    printf("%g\n",a);
    return 0;
}
